{
  "compilerOptions": {
    "baseUrl": ".",
    "strict": true,
    "outDir": "dist",
    "sourceMap": true,
    "target": "esnext",
    "moduleResolution": "node",
    "module": "esnext",
    // TODO: needed for "resolver.vue", until we transform it to a render function or find a way to compile it during build
    "allowJs": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
